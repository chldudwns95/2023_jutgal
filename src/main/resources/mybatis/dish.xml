<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dish">

    <insert id="insSub">
        INSERT INTO DISH_DTL_SUB
        (
             SUBTITLE_CODE
            ,SUBTITLE_NAME
        )
        VALUES
        (
         #{SUBTITLE_CODE}
        ,#{SUBTITLE_NAME}
        )
    </insert>

</mapper>